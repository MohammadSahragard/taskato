import { Button } from '@nextui-org/react';

const TodoDetailsSaveBtn = () => {
    return (
        <Button
            color='primary'
            radius='sm'
        >
            Save changes
        </Button>
    );
};

export default TodoDetailsSaveBtn;
