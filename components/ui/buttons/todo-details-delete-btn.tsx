import { Button } from '@nextui-org/react';

const TodoDetailsDeleteBtn = () => {
    return (
        <Button
            variant='ghost'
            color='danger'
            radius='sm'
        >
            Delete task
        </Button>
    );
};

export default TodoDetailsDeleteBtn;
